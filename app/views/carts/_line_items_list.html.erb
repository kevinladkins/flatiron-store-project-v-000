<p><%= render partial: 'items/links', locals: {item: line_item.item}%> <%= price(line_item.item) %> | Quantity: <%= line_item.quantity %> | <% if line_item.item.inventory_low? %>
    <strong>There are only <%= line_item.item.inventory %> left in stock.</strong>  <% end %> </p>
    
    
